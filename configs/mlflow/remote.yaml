# @package mlflow

# =============================== MLflow Configuration ===================== #
experiment_name: "e2e-ml-remote"
run_name: "run_{now:%Y%m%d_%H%M%S}"
tracking_uri: "http://localhost:5000" # Remote MLflow server

# =============================== Logging ================================== #
log_artifacts: true
log_models: true
log_metrics: true
log_params: true

# =============================== Model Registry ========================== #
model_registry:
  enabled: true
  model_name: "cifar10-classifier-remote"
  stage: "None"

# =============================== Artifacts =============================== #
artifacts:
  model_dir: "${paths.model_dir}"
  plots_dir: "${paths.output_dir}/plots"
  logs_dir: "${paths.log_dir}"

# =============================== Metrics to Track ======================== #
metrics:
  - "train_loss"
  - "val_loss"
  - "train_accuracy"
  - "val_accuracy"
  - "learning_rate"
  - "epoch"

# =============================== Parameters to Track ===================== #
params:
  - "model.input_channels"
  - "model.num_classes"
  - "model.dropout_rate"
  - "training.epochs"
  - "training.learning_rate"
  - "training.weight_decay"
  - "data.batch_size"
